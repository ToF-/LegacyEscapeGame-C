to compile the rent program:

`make rent`

to compile and run the tests:

`make tests`

run the program on a sample: `./sample`

run the program on a small set: `./small`

run the program on a large set: `./large`
